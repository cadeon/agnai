version: '2'

services:
  mongo:
    image: mongo:8
    restart: always
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7
    restart: always
    ports:
      - '6379:6379'

volumes:
  mongo-data: